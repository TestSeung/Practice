#include<iostream>

using namespace std;

int main() {
	long long num;
	long long result = 0;
	cin >> num;
	result =(num -2)* (num - 1)* num / 6;
	
	cout << result << '\n';

	cout << 3;
}